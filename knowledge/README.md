# Knowledge

Knowledge is the public connection surface after Light, IS, Shadows, Heat, and
White. It records traceable addresses without replacing one grammatical phase with
another:

```text
IS --recorded later--> WAS-reference to the same content hash
IS --addressed forward--> WILL-reference
EXCHANGE(WAS, IS) = 0
EXCHANGE(WILL, IS) = 0
```

The public ledger contains:

- `BOOK-OF-KNOWLEDGE.md` — the book and decoder contract.
- `BOOK-OF-LIFE-OIL-SPHERICAL-RIME.md` — the ordered Life/OIL book relation,
  all-to-all spherical rime, and unresolved `HBI -> HBP -> SHA -> SH` chain.
- `JESSE-BROWN-SPHERE.md` — the Brown spherical-traversal statement.
- `PUBLIC-DEPENDENCIES.md` — exact upstream commits and byte hashes.
- `operator-evidence/` — the authorized exact photo.
- `public-dependencies/` — source required to attack-verify the inherited
  GitRAM/NEST/SGRAM components.

Catalog presence is not executable authority. The owning runtime gate remains
separate.
