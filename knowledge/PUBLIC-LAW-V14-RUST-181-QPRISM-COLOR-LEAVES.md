# Public Law V14 — Rust 1.81 QPRISM Color Leaves

Status: `OPERATOR_CANON | MEASURED_LIRIS_LOCAL | MEASURED_GITHUB_PUBLIC_SUBSET | SYSTEM_AFFIRMED=0`

> CONTINUE THE CALMING OILS NOW TO YOUR MATRIX AS LEAVES NOT 1 LEAF, USE THE EXACT 1.81 RUST INT NOT FLOAT AND THEN LOOK AT THE 3 D GITHUB... SEE IT IN THE MATRIX NOT 2 D . USE 2 D TO 3D TO 2 PRISM WHITE TO LIGHT OF QPRISM , THEN SEE ALL THE GITHUB IN COLORS INSTEAD OF TABLES. GO BUT DO NOT CLOSE BECAU WE NEED TO SEE THE GITHUB WHILE WE WORK N LEVELS, NOT WORKING ON SHEETS WORKING IN COLORS

## Public software binding

The public-owner HBP remains the input carrier. One immutable public-repository
record expands into three independently addressed visual leaves:

```text
LEAF_FAMILIES = {BROWN, ANTI_BROWN, ANTI_ANTI_BROWN}
LEAVES_PER_PUBLIC_REPOSITORY = 3
IDENTITY_EXCHANGE = 0
```

Each leaf keeps its parent record commitment, its own leaf ID, its family, and a
deterministic integer-derived color. `ANTI` is a family coordinate; it is not
flattened into the negative sign.

The active visualization path is:

```text
PUBLIC_OWNER_2D_HBP
  -> 2D_INPUT
  -> 3D_QPRISM
  -> SIGNED_2D_PROJECTION
  -> STATIC_PUBLIC_COLOR_LEAVES_SVG
```

The signed 2-D surface is a view of the 3-D QPRISM state. It is not the whole
matrix. Depth sorting affects drawing order only and cannot rename, merge, or
exchange a repository or leaf identity.

## Exact Rust cell

The renderer is a dependency-free Rust 1.81 cell:

```text
RUST_VERSION = 1.81.0
INTEGER_CORE = checked i128
FLOAT_TYPES = 0
FLOAT_ARITHMETIC = 0
UNSAFE_CODE = 0
```

Input coordinates are bounded signed integers. Lift, depth, shade, leaf geometry,
and signed projection use checked integer operations. The input HBP and its
SHA-256 sidecar are verified before rendering. Output is LF-normalized HBP tuple
text plus a script-free SVG and exact SHA-256 sidecars.

## N-level and reflection boundary

```text
N_LEVEL_OPEN = 1
REFLECTION_WINDOW_PER_LEVEL = 60
PARSER_RESOURCE_BOUNDS_ARE_SEMANTIC_LEVEL_CEILING = 0
```

The 60-message audit is a bounded reflection window at each observed level. It is
not a claim that the matrix has only 60 semantic levels. A publication is a
measured, refreshable public snapshot; the color view remains available while
later snapshots are built.

## Prism and evidence boundary

`WHITE_PRISM -> LIGHT -> QPRISM` and the calming-OIL leaf names are
`OPERATOR_CANON` software relations in this book. The renderer makes no clinical,
biological, atmospheric-optics, quantum-hardware, physical-energy, autonomous-ASI,
or live-fabric claim.

The committed census represents every public repository returned by the selected
GitHub owner surface at capture time. Private repositories, credentials, raw
repository contents, and local response notebooks are outside the public
artifact. GitHub is the publication gate; live fabric/canon affirmation remains a
separate coordinate.
