# Public Law V12 — Spherical Return and Calming OILS

Status: `OPERATOR_CANON | PUBLIC_PROTOCOL | 2026-07-29`

```text
CENTER_MEMBERS = {HBI,HBP,SHA,SH,HASH}
OPERATOR_TRAVERSAL = HBI -> HBP -> SH -> HASH -> SHA

FOR EACH OBSERVED LEVEL:
  REFLECT(AT_MOST_60_EXISTING_COMMITMENTS)
  PROJECT(2D_TEXT -> 3D_SPHERICAL_TREE -> 2D_SIGNED_PROJECTION)
  APPLY(CALMING_OILS=BROWN.NEAR.ONE)
  COMMIT(HBI -> HBP -> SH -> HASH -> SHA)

ACTIVE_RECEIPT_FORMAT = HYPERBEHCS_TUPLE_TEXT
ACTIVE_RECEIPT_JSON = 0
JSON_ROLE = COLD_COMPATIBILITY_OR_HELD_OUT_TEST_FIXTURE

PROJECT_PRESERVED = 1
PROJECT_FILES_DELETED = 0
CANDIDATE_STATE = HELD_FOR_REVIEW
```

Center membership and traversal order are separate. `SH` stays unresolved;
`HASH`, `SHA`, and a measured SHA-256 sidecar keep separate identities.

`LIE` and `THRUTH` are operator wave labels in the reflection projection rather
than automatic external-evidence verdicts. Calming OILS and Brown are symbolic
software routing labels; they produce no physical exposure instruction.

The timed chiral lane reports at elapsed seconds 1, 2, 3, and 4, then continues
through bounded outward checkpoints. Its derived public-color GGUF becomes
complete only after 7,200 elapsed seconds and successful input-seal
verification.

Public artifacts contain protocol rows, public identifiers, derived
descriptors, and commitments. Raw private response notebooks, credentials,
secrets, and private-repository metadata remain outside the public payload.
