# Hashes

`PINNED-SOURCES.sha256` seals the exact public dependency bytes and authorized
operator photo embedded in this repository. Paths are repository-relative and use
forward slashes. Text inputs are LF-normalized before sealing.

The manifest proves byte identity for the named public slice. It does not by itself
prove a live runtime, a physical interpretation, or system-wide absorption.
